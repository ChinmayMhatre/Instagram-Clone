import React from 'react'
import { View, Text } from 'react-native'

const Save = (props) => {
    console.log(props.route.params.image)
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Save
